# The Ramsey community number as a renormalization-group crossing

Reproducibility package for the manuscript

> **The Ramsey community number as a renormalization-group crossing**

submitted to *Physica A: Statistical Mechanics and its Applications*
(Elsevier `elsarticle`, `preprint,12pt`). Third in
the "wants to be broken" series, after the ring and the pseudofractal web.

## What is shown

On the diamond hierarchical lattice (the `(b,s)=(2,2)` Migdal–Kadanoff cell,
equivalently the `(2,2)`-flower) the Ramsey community number `r_κ` is an **exact
renormalization-group threshold crossing** rather than a fitted slope:

* the block-pair sufficient statistics of the self-similar bundle cut obey an
  exact **linear** RG map `M` with spectrum `{λ_vol, λ_seam} = {bs, b}`
  (volume rate `bs`, seam rate `b`; gap `1/s`);
* the degree-corrected block-model evidence flows to a community fixed point
  `(u_in, u_cross) = (K, 0)` with evidence density `log R_dc / m → ln K`
  (lattice-independent; cross-checked against the pseudofractal 3-way cut);
* `r_κ` is the generation at which the running evidence crosses
  `ln[q/(1−q)]`: `r_κ = 12` (q=0.5) and `44` (q≥0.9) for the degree-corrected
  model, vs `684` for the plain model. A closed form `r_κ(b,s;q)` is derived
  for the whole family, with an explicit finite-size correction.

The paper then places two spin models on the same lattice and shows the
community partition is not merely detectable but **thermodynamically ordered**:

* the `q=2` Reichardt–Bornholdt Hamiltonian has an exact community-ordered
  phase — below the ferromagnetic critical temperature (`k_B T_c/J = 1.641`)
  the two hubs lock into opposite communities for any resolution `γ>0`, and the
  order persists as `n→∞`;
* the optimal partition is a nested hierarchy of `q_opt ∼ √N` communities, so
  the number of Potts states that best describes the network grows with the
  network;
* that hierarchy orders thermally level by level, through a cascade of
  first-order Potts transitions whose temperatures fall as `1/ln q`
  (`k_B T_c/J ∼ 1/(c ln q)`, `c ≈ 0.70`), so every stable level persists as
  `n→∞`.

## Files

### Manuscript and submission

| file | description |
|------|-------------|
| `manuscript.tex` | Elsevier `elsarticle` source (`preprint,12pt`) — the full paper, all derivations in-text |
| `manuscript.pdf` | compiled manuscript |
| `manuscript.bbl` | processed bibliography (include with submission) |
| `refs.bib`       | BibTeX references |
| `cover_letter.txt` | cover letter to the *Physica A* editor (plain text) |

### Tables

All three manuscript tables are reproduced — from closed forms, checked against
direct construction of the lattice — by **`diamond_rg.py`**:

| table | content | source |
|-------|---------|--------|
| Table I  (`tab:counts`)   | closed-form block-pair statistics of the bundle cut | `diamond_rg.py` |
| Table II (`tab:evidence`) | log evidence vs generation, degree-corrected and plain, and the density `logR_dc/m → ln 2` | `diamond_rg.py` |
| Table III (`tab:rk`)      | Ramsey community number `r_κ(q) = N_{t*}` | `diamond_rg.py` |

### Figures

| figure | file | generated by (→ backend) |
|--------|------|--------------------------|
| Fig. 1 (`fig:lattice`)      | `fig_lattice.pdf`      | `make_figures.py` (→ `diamond_rg.py`) |
| Fig. 2 (`fig:construction`) | `fig_construction.pdf` | `make_construction_figure.py` (→ `diamond_rg.py`) |
| Fig. 3 (`fig:data`)         | `fig_data.pdf`         | `make_figures.py` (→ `diamond_rg.py`) |
| Fig. 4 (`fig:rb`)           | `fig_rb.pdf`           | `make_rb_figure.py` (→ `rb_fast.py`, `rb_exact_gf.py`) |
| Fig. 5 (`fig:hier`)         | `fig_hier.pdf`         | `make_hier_figure.py` (→ `rb_potts_hier.py`) |
| Fig. 6 (`fig:potts`)        | `fig_potts.pdf`        | `make_potts_figure.py` (→ `rb_potts_decimation.py`) |

`fig_construction.pdf` is the `b=s=2` edge-replacement cartoon of Sec. II
(generations `t=0..3`), with node/edge counts cross-checked against
`diamond_rg.diamond()`. `make_figures.py` also emits `fig_evidence.pdf` and
`fig_flow.pdf`, the two panels of Fig. 3 as standalone files; these are
**not** used by the manuscript.

### Computation and verification

| file | role |
|------|------|
| `diamond_rg.py`  | core module: builds the `(b,s)` diamond by edge replacement, evaluates the degree-corrected and plain evidences on any graph/partition, carries the exact RG map `M`, and reproduces Tables I–III against direct construction through `t=7`. Importable; run as a script it prints every table, slope, the spectrum, and `r_κ`. |
| `general_bs.py`  | verifies the general-`(b,s)` results: bundle lemma, per-bundle recursion `(c'=bc, i'=bs·i+b(s−1)c)`, block statistics, density ratios and fixed point, evidence density `ln b` with the finite-size correction `δ_t`, and the crossing-formula window (`b≤4, s≤5`). |
| `check_rk_bs.py` | verifies the size formula `N_t(b,s)` and the `r_κ(b,s;q)` crossing formula. |
| `probe_bs.py`    | numerical check that the seam eigenvalue is `b` (not `s`) across `(b,s) ∈ {(2,2),(3,2),(4,2),(2,3),(2,4),(3,3)}`. |

### Thermodynamics backends (used by Figs. 3–5)

| file | role |
|------|------|
| `rb_fast.py` / `rb_exact_gf.py` | exact decimation of the `q=2` Reichardt–Bornholdt (community) Ising model; hub anti-alignment order parameter and `T_c^{FM}` (Fig. 3). |
| `rb_potts_hier.py`      | depth-`d` nested-partition modularity `Q(d)` and the optimal depth `d_⋆`, `q_opt ∼ √N` (Fig. 4). |
| `rb_potts_decimation.py`| exact Potts-bond series/parallel decimation for the finite-temperature order of the growing-`q` hierarchy, `T_c(q) ∼ 1/ln q` (Fig. 5). |

### Exploratory / superseded (not used by the final manuscript)

Kept for the record; not wired to any manuscript table or figure:
`ising_diamond.py`, `ising_analysis.py`, `ising_domains.py`, `ising_hier.py`,
`ising_hier2.py`, `ising_staggered.py`, `make_ising_figure.py`,
`rb_decimation.py`, `rb_exact2.py`, `rb_hier_thermo.py`,
`rb_ising_derivation.py`, `rb_ising_mft.py`, `rb_mft_exact.py`,
`rb_potts_thermo.py`.

### Build

| file | description |
|------|-------------|
| `Makefile` | `make` builds the five figures and compiles `manuscript.pdf`; `make verify` runs all checks; `make figures` builds figures only; `make clean` removes build products. |

## Reproduce

Requirements: Python 3 with `numpy`, `networkx`, `mpmath`, `matplotlib`; a TeX
Live installation including Elsevier `elsarticle` (Debian/Ubuntu package
`texlive-publishers`).

```sh
pip install numpy networkx mpmath matplotlib
make verify     # prints all tables, slopes, the RG map spectrum, and r_kappa
make            # regenerates the five figures and compiles manuscript.pdf
```

Running `python3 diamond_rg.py` alone reproduces, from scratch and checked
against direct construction of the lattice through `t=7`: the closed-form
block-pair counts, the exact RG map and its eigenvalues `{bs, b}`, the evidence
of both models (Tables I–III), the density fixed point `ln K`, the density
flow, and the Ramsey numbers. All arithmetic is at 80 digits (`mpmath`).

## Notes

* **Author / affiliation** are carried over from the companion papers and should
  be finalized before submission.
* The lattice exists only at discrete sizes `N_t = (2·4^t + 4)/3`, so `r_κ` is
  necessarily one of these values.
* The `(b,s)` generalization: `diamond(t, b, s)` in `diamond_rg.py` builds the
  general lattice; `probe_bs.py` verifies `λ_vol = bs`, `λ_seam = b` for
  `(b,s) ∈ {(2,2),(3,2),(4,2),(2,3),(2,4),(3,3)}`.
